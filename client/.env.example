# Camara local. Nunca subir el .env real a Git.
CAMERA_NAME=CAMARA_01
CAMERA_RTSP_URL=rtsp://usuario:clave@192.168.1.100:554/stream1
RECONNECT_SECONDS=3

# Valores locales de respaldo. En cliente V3 administrado,
# el servidor define la sucursal y camara reales.
BRANCH_ID=1

# URL central. En otra sucursal NO usar 127.0.0.1.
# IMPORTANTE: debe terminar en /api.
API_URL=http://127.0.0.1:8000/api
API_TIMEOUT_SECONDS=5

# V3: copiar EXACTAMENTE los valores generados desde
# Administracion > Clientes. No usar valores de ejemplo.
CLIENT_ID=
CLIENT_TOKEN=

# Compatibilidad V2; dejar vacio en clientes V3.
API_TOKEN=

# Interfaz del cliente.
# false = muestra la camara; true = totalmente oculto.
HEADLESS=false
# true = al minimizar o cerrar la X, queda en iconos ocultos de Windows.
TRAY_MODE=true

# Rendimiento cuando la ventana esta abierta para configuracion/diagnostico.
PROCESS_FPS=12
YOLO_IMGSZ=640

# Perfil automatico de produccion cuando queda en iconos ocultos o HEADLESS.
# Para no perder cruces rapidos se usan 5-8 FPS (6 recomendado) y 416 px.
# El motion gate reduce inferencias cuando la zona del trazado esta quieta.
# YOLO_CPU_THREADS=1 prioriza bajo consumo; usar 2 en una PC de escritorio si
# se necesita mas velocidad de inferencia.
BACKGROUND_PROCESS_FPS=6
BACKGROUND_YOLO_IMGSZ=416
YOLO_CPU_THREADS=1

APP_VERSION=3.1.0
SYNC_INTERVAL_SECONDS=5
SYNC_BATCH_SIZE=100
HEARTBEAT_INTERVAL_SECONDS=20
REMOTE_CONFIG_INTERVAL_SECONDS=30
